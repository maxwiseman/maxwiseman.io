---
title: "test2"
date: "2018-07-15"
---
import { useState } from 'react'
import { Card, CardContent } from '../../components/ui/card'
import { Button } from '../../components/ui/button'

# Read Here

https://google.com

- Make an mdx file
  - Add some content
- Add some content

1. Do something
    1. Do something else
2. Do something else

export const MyDynamicButton = () => {
  const [count, setCount] = useState(0);
  return (
    <Button className='mb-4' onClick={() => setCount(count + 1)}>Clicked {count} times!</Button>
  );
};

<div className='my-4'><Button variant='outline'>Test Hello</Button></div>
<MyDynamicButton />

```ts showLineNumbers  "input" title="app/index.ts"
export default function test(input: string): string {
  return input
}

export default function test(input: string): string {
  return input
}
```

```sh
cd .. && bun run dev
```


this is the function! It returns the input string.

<Card><CardContent>Hello from a card</CardContent></Card>
